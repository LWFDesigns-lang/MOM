# WARNING: NEVER USE THIS FILE IN PRODUCTION OR COMMIT REAL KEYS
# This is a placeholder template only. Real credentials must live in
# AWS Secrets Manager (https://docs.aws.amazon.com/secretsmanager/latest/userguide/intro.html)
# Use the AWS Secrets Manager JIT pattern described in security-config.md.

BRAVE_API_KEY=PLACEHOLDER_BRAVE_API_KEY  # ⚠️ Replace with AWS SM reference; do not paste real key here.
ETSY_API_KEY=PLACEHOLDER_ETSY_API_KEY     # AWS Secrets Manager: prod/etsy-api-key
ETSY_API_SECRET=PLACEHOLDER_ETSY_API_SECRET  # AWS Secrets Manager: prod/etsy-api-secret
PRINTFUL_API_KEY=PLACEHOLDER_PRINTFUL_API_KEY  # OAuth-only (no static keys stored anywhere)
SERPAPI_KEY=PLACEHOLDER_SERPAPI_KEY  # Optional (trends fallback); treat as sensitive and rotate frequently.
QDRANT_API_KEY=PLACEHOLDER_QDRANT_API_KEY  # Localhost-only service; store in AWS SM and inject at runtime.
QDRANT_READONLY_KEY=PLACEHOLDER_QDRANT_READONLY_KEY
NEO4J_PASSWORD=PLACEHOLDER_NEO4J_PASSWORD  # Used only for Docker localhost Neo4j; rotate via Secrets Manager.
SHOPIFY_STORE=PLACEHOLDER_SHOPIFY_STORE  # Future integration (avoid filling in this file for now).
SHOPIFY_TOKEN=PLACEHOLDER_SHOPIFY_TOKEN

# Security reminders
# - Do NOT commit this file with real credentials; Git ignore .env (per SETUP_GUIDE_ARCHITECTURE.md & security-config).
# - Use AWS Secrets Manager + MCP JIT retrieval for every OAuth provider (SEP-835 / SEP-1319 compliance).
# - Rotate tokens at least every 60 minutes (60 minute TTL enforced by MCP security config).
# - Any script referencing these must warn "This file is for template only; pull secrets at runtime."